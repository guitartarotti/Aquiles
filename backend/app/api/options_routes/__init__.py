"""Route modules grouped by options routes use case."""

from . import market, modeling, open_interest, snapshots, volatility, volume

_ROUTE_MODULES = (market, modeling, open_interest, snapshots, volatility, volume)
